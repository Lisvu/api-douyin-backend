# F02 / F03 组员 A 自测报告

> **负责人**：组员 A  
> **关联文档**：`F02-F03-推荐流接口草稿.md`、`F02-F03-观看记录与切换方案.md`、`API_DOC_TEMPLATE.md`  
> **Swagger**：http://localhost:8080/swagger-ui/index.html

---

## 1. 交付物对照（`项目分工.md`）

| 交付项 | 状态 | 证明 |
| --- | --- | --- |
| F02 推荐按点赞降序 | ✅ | `VideoRepository` JPQL + 自测脚本 |
| F02 观看去重 | ✅ | `POST views` + 再次 GET 条数减少 |
| F03 上下切换 | ✅ | `App.jsx` 滚轮/箭头 + `POST views` |
| 接口符合 API 规范 | ✅ | REST 路径 + 状态码 |
| 共享 API 文档 | ✅ | `API_DOC_TEMPLATE.md` §F02/F03 |
| 异常场景自测 | ✅ | 401 / 404 + 单元测试 |
| ≥3 条测试视频 | ✅ | 种子数据 6 条 |

---

## 2. Swagger 自测步骤

1. SSH 隧道 + `mvn spring-boot:run`
2. 打开 http://localhost:8080/swagger-ui/index.html
3. 登录拿 token → Authorize
4. 测试 **F02/F03 推荐流** 三个接口

---

## 3. 命令行自测

```powershell
cd docs/self-test-screenshots
.\run-f02-f03-selftest.ps1
```

生成 `00-summary.json` 及分步 JSON 响应。

---

## 4. 异常场景清单

| 场景 | 接口 | 预期 |
| --- | --- | --- |
| 成功 | GET recommendations | 200，`videos` 非空 |
| 未登录 | GET recommendations | 401 |
| 去重 | POST views → GET | 已看 id 不在列表 |
| 视频不存在 | POST views/999999 | 404 |
| 重置 | DELETE views → GET | 列表恢复 |
| F03 切换 | 前端滚轮 | 监控有 POST views |

---

## 5. 单元测试

```powershell
mvn test -Dtest=RecommendFeedApiTest
```

覆盖：推荐列表字段、404、观看幂等、重置观看。

---

## 6. 前端联调

```powershell
cd ../api-douyin-frontend
npm run dev
```

访问 http://localhost:5173 ，账号 `douyin_creator` / `password123`。

验收：推荐加载 → 滚轮切换 → 右侧监控出现 `GET recommendations` 与 `POST views`。
