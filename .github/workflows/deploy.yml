name: Deploy to Server

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: 165.232.172.99
          username: lihao
          password: ${{ secrets.SSH_PASSWORD }}
          script: /home/lihao/deploy.sh
